<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Menu - Aziza Bakery</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#d35400',
              secondary: '#e67e22',
              cream: '#f8f0e3',
              gold: '#d4af37',
            },
            fontFamily: {
              'display': ['Playfair Display', 'serif'],
              'body': ['Inter', 'sans-serif'],
            }
          }
        }
      }
    </script>
    <style>
      .nav-link {
        position: relative;
      }
      .nav-link::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 2px;
        background: #d4af37;
        transition: width 0.3s ease;
      }
      .nav-link:hover::after,
      .nav-link.active::after {
        width: 100%;
      }
      
      .navbar-scrolled {
        background: rgba(17, 24, 39, 0.95);
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      }
      
      .mobile-menu {
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease;
      }
      .mobile-menu.open {
        max-height: 500px;
      }
      
      .menu-card {
        animation: fadeInUp 0.6s ease forwards;
        opacity: 0;
      }
      
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      
      .menu-card:nth-child(1) { animation-delay: 0.1s; }
      .menu-card:nth-child(2) { animation-delay: 0.2s; }
      .menu-card:nth-child(3) { animation-delay: 0.3s; }
      .menu-card:nth-child(4) { animation-delay: 0.4s; }
      .menu-card:nth-child(5) { animation-delay: 0.5s; }
      .menu-card:nth-child(6) { animation-delay: 0.6s; }
      
      .menu-card img {
        transition: transform 0.5s ease;
      }
      .menu-card:hover img {
        transform: scale(1.08);
      }
    </style>
</head>
<body class="font-body bg-cream min-h-screen">

<!-- Navigation -->
<nav id="navbar" class="fixed top-0 left-0 right-0 z-50 bg-gray-900 transition-all duration-300">
  <div class="container mx-auto px-6 lg:px-12">
    <div class="flex justify-between items-center py-5">
      <a href="/" class="text-white text-2xl lg:text-3xl font-display font-bold tracking-wide hover:text-gold transition-colors duration-300">
        Aziza Bakery
      </a>
      
      <div class="hidden md:flex items-center space-x-8 lg:space-x-10">
        <a href="/" class="nav-link text-white/90 hover:text-white transition-colors duration-300 pb-1">Home</a>
        <a href="/gallery" class="nav-link text-white/90 hover:text-white transition-colors duration-300 pb-1">Gallery</a>
        <a href="/menu" class="nav-link active text-white font-semibold hover:text-gold transition-colors duration-300 pb-1">Menu</a>
        <a href="/order" class="nav-link text-white/90 hover:text-white transition-colors duration-300 pb-1">Order</a>
        <a href="/contact" class="nav-link text-white/90 hover:text-white transition-colors duration-300 pb-1">Contact</a>
        <a href="/cart" class="nav-link text-white/90 hover:text-white transition-colors duration-300 pb-1 flex items-center">
          🛒 Cart (<span id="cart-count">0</span>)
        </a>
        <a href="/admin" class="px-5 py-2 bg-gold/20 hover:bg-gold/30 text-white rounded-full text-sm font-medium transition-all duration-300 border border-gold/30">
          Admin
        </a>
      </div>
      
      <button id="mobile-menu-btn" class="md:hidden text-white focus:outline-none">
        <svg id="menu-icon" class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
        <svg id="close-icon" class="w-7 h-7 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>
    
    <div id="mobile-menu" class="mobile-menu md:hidden">
      <div class="py-4 space-y-4 border-t border-white/10">
        <a href="/" class="block text-white/90 hover:text-white transition-colors duration-300 py-2">Home</a>
        <a href="/gallery" class="block text-white/90 hover:text-white transition-colors duration-300 py-2">Gallery</a>
        <a href="/menu" class="block text-white font-semibold hover:text-gold transition-colors duration-300 py-2">Menu</a>
        <a href="/order" class="block text-white/90 hover:text-white transition-colors duration-300 py-2">Order</a>
        <a href="/cart" class="block text-white/90 hover:text-white transition-colors duration-300 py-2">Cart</a>
        <a href="/contact" class="block text-white/90 hover:text-white transition-colors duration-300 py-2">Contact</a>
        <a href="/admin" class="block text-white/90 hover:text-white transition-colors duration-300 py-2 border-t border-white/10 pt-4">Admin Panel</a>
      </div>
    </div>
  </div>
</nav>

<!-- Header -->
<section class="pt-32 pb-16 bg-gradient-to-br from-primary via-secondary to-primary">
  <div class="container mx-auto px-6 lg:px-12 text-center">
    <span class="inline-block bg-white/20 backdrop-blur-sm text-white px-6 py-2 rounded-full text-sm font-medium tracking-wider uppercase mb-6 border border-white/30">
      Our Delicious Menu
    </span>
    <h1 class="font-display text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6">
      Menu
    </h1>
    <p class="text-white/95 text-xl md:text-2xl max-w-2xl mx-auto">
      Handcrafted daily with premium ingredients and passion
    </p>
  </div>
</section>

<!-- Menu Grid -->
<section class="py-20 lg:py-24">
  <div class="container mx-auto px-6 lg:px-12">
    
    <% if (products.length > 0) { %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 lg:gap-10">
        <% products.forEach((product, index) => { %>
          <div class="menu-card group bg-white rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 overflow-hidden">
            <!-- Image -->
            <div class="relative overflow-hidden aspect-[4/3]">
              <img 
                src="/images/<%= product.image %>" 
                alt="<%= product.name %>" 
                class="w-full h-full object-cover"
                loading="lazy"
              >
              <% if (index < 3) { %>
                <span class="absolute top-4 right-4 bg-gold text-white px-4 py-1.5 rounded-full text-xs font-bold uppercase tracking-wide shadow-lg">
                  New
                </span>
              <% } %>
              <div class="absolute inset-0 bg-gradient-to-t from-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            </div>
            
            <!-- Content -->
            <div class="p-8">
              <!-- Name & Price -->
              <div class="flex justify-between items-start mb-4">
                <h3 class="font-display text-2xl lg:text-3xl font-bold text-gray-900 flex-1 pr-4">
                  <%= product.name %>
                </h3>
                <span class="text-2xl lg:text-3xl font-bold text-primary whitespace-nowrap">
                  $<%= product.price.toFixed(2) %>
                </span>
              </div>
              
              <!-- Description -->
              <p class="text-gray-600 leading-relaxed mb-6 text-base">
                <%= product.description %>
              </p>
              
              <!-- Divider -->
              <div class="border-t border-gray-100 mb-6"></div>
              
              <!-- Add to Cart Button -->
              <button onclick="addToCart('<%= product._id %>')" class="w-full bg-primary hover:bg-secondary text-white font-bold py-4 px-6 rounded-xl transition-all duration-300 hover:scale-105 hover:shadow-lg flex items-center justify-center group text-decoration-none">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-1.5 6M7 13l-1.5 6m0 0h9m-9 0h9"/>
                </svg>
                <span>Add to Cart</span>
              </button>
            </div>
          </div>
        <% }) %>
      </div>
      
    <% } else { %>
      <div class="text-center py-20 bg-white rounded-3xl shadow-lg max-w-2xl mx-auto">
        <div class="text-8xl mb-8">🍰</div>
        <h3 class="font-display text-3xl font-bold text-gray-800 mb-4">Menu Coming Soon</h3>
        <p class="text-gray-600 text-lg mb-8">We're preparing our delicious menu for you!</p>
        <a href="/" class="inline-flex items-center justify-center px-8 py-4 bg-primary hover:bg-secondary text-white font-bold rounded-full transition-all duration-300">
          Return to Home
        </a>
      </div>
    <% } %>
    
  </div>
</section>

<!-- Premium Footer -->
<footer class="bg-gray-900 text-white">
  <div class="container mx-auto px-6 lg:px-12 py-16 lg:py-20">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 lg:gap-16 mb-12">
      
      <!-- Brand Section -->
      <div class="lg:col-span-1">
        <h3 class="font-display text-3xl lg:text-4xl font-bold mb-4 text-gold">Aziza Bakery</h3>
        <p class="text-gray-400 leading-relaxed mb-6">
          Crafting memorable moments through exceptional baked goods since 2020. Every creation is made with love and the finest ingredients.
        </p>
        <!-- Social Media Icons -->
        <div class="flex space-x-4">
          <a href="#" class="w-10 h-10 bg-white/10 hover:bg-gold rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
            </svg>
          </a>
          <a href="#" class="w-10 h-10 bg-white/10 hover:bg-gold rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
            </svg>
          </a>
          <a href="#" class="w-10 h-10 bg-white/10 hover:bg-gold rounded-full flex items-center justify-center transition-all duration-300 hover:scale-110">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/>
            </svg>
          </a>
        </div>
      </div>
      
      <!-- Quick Links -->
      <div>
        <h4 class="font-semibold text-lg mb-6 text-white">Quick Links</h4>
        <ul class="space-y-3">
          <li><a href="/" class="text-gray-400 hover:text-gold transition-colors duration-300 flex items-center group">
            <span class="mr-2 group-hover:mr-3 transition-all">→</span> Home
          </a></li>
          <li><a href="/gallery" class="text-gray-400 hover:text-gold transition-colors duration-300 flex items-center group">
            <span class="mr-2 group-hover:mr-3 transition-all">→</span> Gallery
          </a></li>
          <li><a href="/menu" class="text-gray-400 hover:text-gold transition-colors duration-300 flex items-center group">
            <span class="mr-2 group-hover:mr-3 transition-all">→</span> Menu
          </a></li>
          <li><a href="/contact" class="text-gray-400 hover:text-gold transition-colors duration-300 flex items-center group">
            <span class="mr-2 group-hover:mr-3 transition-all">→</span> Contact
          </a></li>
          <li><a href="/admin" class="text-gray-400 hover:text-gold transition-colors duration-300 flex items-center group">
            <span class="mr-2 group-hover:mr-3 transition-all">→</span> Admin
          </a></li>
        </ul>
      </div>
      
      <!-- Contact Info -->
      <div>
        <h4 class="font-semibold text-lg mb-6 text-white">Contact Us</h4>
        <ul class="space-y-4 text-gray-400">
          <li class="flex items-start">
            <svg class="w-5 h-5 mr-3 mt-1 text-gold flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <span>123 Bakery Street<br/>Sweet City, SC 12345</span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 mr-3 text-gold flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
            </svg>
            <span>+1 (555) 123-4567</span>
          </li>
          <li class="flex items-center">
            <svg class="w-5 h-5 mr-3 text-gold flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <span>hello@azizabakery.com</span>
          </li>
        </ul>
      </div>
      
      <!-- Opening Hours -->
      <div>
        <h4 class="font-semibold text-lg mb-6 text-white">Opening Hours</h4>
        <ul class="space-y-3 text-gray-400">
          <li class="flex justify-between">
            <span>Monday - Friday</span>
            <span class="text-gold font-medium">7AM - 8PM</span>
          </li>
          <li class="flex justify-between">
            <span>Saturday</span>
            <span class="text-gold font-medium">8AM - 9PM</span>
          </li>
          <li class="flex justify-between">
            <span>Sunday</span>
            <span class="text-red-400 font-medium">Closed</span>
          </li>
        </ul>
      </div>
      
    </div>
    
    <!-- Bottom Bar -->
    <div class="border-t border-gray-800 pt-8">
      <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
        <p class="text-gray-400 text-sm text-center md:text-left">
          © 2026 Aziza Bakery. All rights reserved. Made with ❤️
        </p>
        <div class="flex space-x-6 text-sm text-gray-400">
          <a href="#" class="hover:text-gold transition-colors">Privacy Policy</a>
          <a href="#" class="hover:text-gold transition-colors">Terms of Service</a>
          <a href="#" class="hover:text-gold transition-colors">Cookie Policy</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  window.addEventListener('scroll', function() {
    const navbar = document.getElementById('navbar');
    if (window.scrollY > 50) {
      navbar.classList.add('navbar-scrolled');
    } else {
      navbar.classList.remove('navbar-scrolled');
    }
  });
  
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('menu-icon');
  const closeIcon = document.getElementById('close-icon');
  
  mobileMenuBtn.addEventListener('click', function() {
    mobileMenu.classList.toggle('open');
    menuIcon.classList.toggle('hidden');
    closeIcon.classList.toggle('hidden');
  });
  
  // Load cart count on page load
  loadCartCount();
  
  async function loadCartCount() {
    try {
      const cart = JSON.parse(localStorage.getItem('cart') || '{"items": [], "totalPrice": 0}');
      const itemCount = cart.items.reduce((total, item) => total + item.quantity, 0);
      const cartCountElement = document.getElementById('cart-count');
      if (cartCountElement) {
        cartCountElement.textContent = itemCount;
      }
    } catch (error) {
      console.error('Error loading cart count:', error);
    }
  }
  
  async function addToCart(productId) {
    console.log('🛒 addToCart called with productId:', productId);
    
    try {
      // Find product info from the page
      const productCard = document.querySelector(`button[onclick="addToCart('${productId}')"]`).closest('.menu-card');
      
      if (!productCard) {
        console.error('Product card not found for productId:', productId);
        return;
      }
      
      const productName = productCard.querySelector('h3').textContent.trim();
      const productPriceText = productCard.querySelector('.text-primary').textContent.replace('$', '');
      const productPrice = parseFloat(productPriceText);
      const productImage = productCard.querySelector('img').src.split('/').pop();
      
      console.log('🛒 Product info:', { productName, productPrice, productImage });
      
      // Send to server to add to session cart
      console.log('🛒 Sending request to server...');
      const response = await fetch(`/cart/add/${productId}`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          quantity: 1
        })
      });
      
      console.log('🛒 Server response status:', response.status);
      const result = await response.json();
      console.log('🛒 Server response:', result);
      
      if (result.success) {
        // Update cart count in navbar
        const cartCountElement = document.getElementById('cart-count');
        if (cartCountElement) {
          cartCountElement.textContent = result.cart.itemCount;
          console.log('🛒 Updated cart count to:', result.cart.itemCount);
        } else {
          console.error('🛒 Cart count element not found');
        }
        
        // Also store in localStorage as backup
        let cart = JSON.parse(localStorage.getItem('cart') || '{"items": [], "totalPrice": 0}');
        
        const existingItemIndex = cart.items.findIndex(item => item.productId === productId);
        
        if (existingItemIndex > -1) {
          cart.items[existingItemIndex].quantity += 1;
        } else {
          cart.items.push({
            productId: productId,
            name: productName,
            price: productPrice,
            quantity: 1,
            image: productImage
          });
        }
        
        cart.totalPrice = cart.items.reduce((total, item) => {
          return total + (item.price * item.quantity);
        }, 0);
        
        localStorage.setItem('cart', JSON.stringify(cart));
        
        // Show success message
        Swal.fire({
          title: 'Added to Cart!',
          text: `${productName} has been added to your cart`,
          icon: 'success',
          timer: 2000,
          showConfirmButton: false,
          toast: true,
          position: 'top-end'
        });
      } else {
        throw new Error(result.message || 'Failed to add to cart');
      }
      
    } catch (error) {
      console.error('Add to cart error:', error);
      Swal.fire('Error', 'Failed to add item to cart: ' + error.message, 'error');
    }
  }
  
  // Load cart count on page load
  async function loadCartCount() {
    try {
      // Get cart count from server
      const response = await fetch('/cart/count');
      const result = await response.json();
      
      if (result.success) {
        const cartCountElement = document.getElementById('cart-count');
        if (cartCountElement) {
          cartCountElement.textContent = result.itemCount;
        }
      } else {
        // Fallback to localStorage
        const cart = JSON.parse(localStorage.getItem('cart') || '{"items": [], "totalPrice": 0}');
        const itemCount = cart.items.reduce((total, item) => total + item.quantity, 0);
        const cartCountElement = document.getElementById('cart-count');
        if (cartCountElement) {
          cartCountElement.textContent = itemCount;
        }
      }
    } catch (error) {
      console.error('Error loading cart count:', error);
      // Fallback to localStorage
      const cart = JSON.parse(localStorage.getItem('cart') || '{"items": [], "totalPrice": 0}');
      const itemCount = cart.items.reduce((total, item) => total + item.quantity, 0);
      const cartCountElement = document.getElementById('cart-count');
      if (cartCountElement) {
        cartCountElement.textContent = itemCount;
      }
    }
  }
</script>

</body>
</html>
